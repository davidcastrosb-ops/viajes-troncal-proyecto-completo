const PUBLIC_HOST = 'viajes.trhoncalhomes.com.mx';
const MASTER_ENDPOINT = process.env.TRHONCAL_MASTER_ENDPOINT ||
  'https://script.google.com/macros/s/AKfycbxq6OxUnMWH004OKyspo7eAbI0GvJvwwDgSnfffSzn9amtKzOWqaDmtWUnrk52rz7U8/exec';

const VALID_STATUS = new Set(['verified','verified-initial','approved','published']);
const FALLBACK_HOTEL_SLUG = {
  'OF-PA-PVR-REV26-001':'friendly-fun-vallarta',
  'OF-PA-PVR-SEP26-002':'barcelo-puerto-vallarta',
  'OF-PA-NAY-REV26-003':'grand-decameron-bucerias'
};
const FALLBACK_SEGMENTS = [
  {offerId:'OF-PA-PVR-REV26-001',segmentKey:'pareja',adults:2,children:0,childAges:[],publicText:'Opción calculada para 2 adultos.',priority:1},
  {offerId:'OF-PA-PVR-SEP26-002',segmentKey:'pareja',adults:2,children:0,childAges:[],publicText:'Opción calculada para 2 adultos.',priority:2},
  {offerId:'OF-PA-NAY-REV26-003',segmentKey:'pareja',adults:2,children:0,childAges:[],publicText:'Precio publicado por persona para una ocupación de 2 adultos.',priority:1}
];
const SEGMENTS = [
  {key:'pareja',eyebrow:'Viajar de a dos',title:'Escapada para dos',description:'Opciones ya calculadas para dos adultos. Elige una para conocer hotel, habitación, servicios y condiciones.'},
  {key:'familia-beneficio',eyebrow:'Viajar en familia',title:'Familia · menores con beneficio',description:'Promociones reales donde la edad y ocupación de los menores generan un beneficio. Las edades y condiciones se muestran de forma explícita.'},
  {key:'juniors',eyebrow:'Viajar en familia',title:'Familias con juniors',description:'Opciones para hijos que ya no entran en la promoción infantil más baja, pero todavía pueden tener tarifa junior o de menor según el proveedor.'}
];

function esc(v=''){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
function safe(v=''){try{const u=new URL(String(v));return /^https?:$/.test(u.protocol)?u.toString():'';}catch(_){return '';}}
function money(v=''){const n=Number(String(v).replace(/[^0-9.-]/g,''));return Number.isFinite(n)?new Intl.NumberFormat('es-MX',{style:'currency',currency:'MXN',maximumFractionDigits:0}).format(n):String(v||'');}
function dateMx(v=''){if(!/^\d{4}-\d{2}-\d{2}$/.test(String(v)))return String(v||'');return new Intl.DateTimeFormat('es-MX',{day:'numeric',month:'short',year:'numeric'}).format(new Date(`${v}T12:00:00`));}
function list(items=[]){return Array.isArray(items)&&items.length?`<ul>${items.slice(0,8).map(x=>`<li>${esc(x)}</li>`).join('')}</ul>`:'<p>Información disponible durante la asesoría.</p>';}
async function loadMaster(){const sep=MASTER_ENDPOINT.includes('?')?'&':'?';const c=new AbortController();const t=setTimeout(()=>c.abort(),10000);try{const r=await fetch(`${MASTER_ENDPOINT}${sep}_ts=${Date.now()}`,{cache:'no-store',redirect:'follow',signal:c.signal,headers:{'User-Agent':'TrhoncalTravel-DestinationHubV3/1.0','Cache-Control':'no-cache'}});if(!r.ok)throw new Error(`Master ${r.status}`);return await r.json();}finally{clearTimeout(t);}}
function fallbackInfer(offer){const occ=String(offer?.occupancy||'').toLowerCase();if(/2\s*adult/.test(occ)&&!/(menor|niñ|junior)/.test(occ))return {offerId:offer.id,segmentKey:'pareja',adults:2,children:0,childAges:[],publicText:offer.occupancy||'Opción para dos adultos.',priority:99};return null;}
function segmentText(seg){if(seg.publicText)return seg.publicText;const ages=Array.isArray(seg.childAges)?seg.childAges.filter(Boolean):[];if(seg.segmentKey==='familia-beneficio')return `${seg.adults||2} adultos${seg.children?` + ${seg.children} menores${ages.length?` de ${ages.join(', ')} años`:''}`:''}. ${seg.childBenefit||'Beneficio infantil sujeto a las condiciones publicadas.'}`;if(seg.segmentKey==='juniors')return `${seg.adults||2} adultos${seg.children?` + ${seg.children} juniors${ages.length?` de ${ages.join(', ')} años`:''}`:''}. ${seg.childFareType||'Tarifa de menor/junior según proveedor.'}`;return 'Opción calculada para 2 adultos.';}
function targetFor(offer,hotels){const hotel=hotels.find(h=>h&&offer.hotelId&&h.id===offer.hotelId);const slug=hotel?.slug||FALLBACK_HOTEL_SLUG[offer.id]||'';return slug?`/hotel-v2/${encodeURIComponent(slug)}?oferta=${encodeURIComponent(offer.id)}`:`/oferta/${encodeURIComponent(offer.id)}`;}
function card(offer,seg,hotels){const image=safe(offer.image),dates=[offer.travelStart?dateMx(offer.travelStart):'',offer.travelEnd?dateMx(offer.travelEnd):''].filter(Boolean).join(' – '),duration=[offer.days?`${offer.days} días`:'',offer.nights?`${offer.nights} noches`:''].filter(Boolean).join(' · '),price=money(offer.price||''),target=targetFor(offer,hotels);return `<article class="hub-offer-card"><a class="hub-offer-image" href="${esc(target)}">${image?`<img src="${esc(image)}" alt="${esc(offer.hotel||offer.title||'Opción de viaje')}" loading="lazy">`:'<div class="hub-image-placeholder">Imagen del hotel en preparación</div>'}</a><div class="hub-offer-body"><div class="hub-offer-meta"><span>${esc(offer.plan||'Viaje')}</span>${dates?`<small>${esc(dates)}</small>`:''}</div><h3 translate="no" class="notranslate">${esc(offer.hotel||offer.title||'Opción de viaje')}</h3><p class="hub-offer-title">${esc(offer.title||'')}</p><div class="hub-offer-facts">${duration?`<span>${esc(duration)}</span>`:''}${offer.occupancy?`<span>${esc(offer.occupancy)}</span>`:''}</div><p class="hub-segment-note">${esc(segmentText(seg))}</p><div class="hub-offer-bottom"><div>${price?`<small>${esc(offer.priceUnit||'Precio publicado')}</small><strong>${esc(price)}</strong><span> MXN</span>`:'<strong>Consultar precio</strong>'}</div><a class="hub-offer-cta" href="${esc(target)}">Ver esta opción →</a></div></div></article>`;}
function groupsFor(offers,segments){const groups=new Map(SEGMENTS.map(s=>[s.key,[]]));const byOffer=new Map();segments.forEach(s=>{if(s?.offerId&&s.segmentKey){if(!byOffer.has(s.offerId))byOffer.set(s.offerId,[]);byOffer.get(s.offerId).push(s);}});offers.forEach(o=>{let rows=byOffer.get(o.id)||[];if(!rows.length){const fb=FALLBACK_SEGMENTS.find(s=>s.offerId===o.id)||fallbackInfer(o);if(fb)rows=[fb];}rows.forEach(seg=>{if(groups.has(seg.segmentKey))groups.get(seg.segmentKey).push({offer:o,seg});});});for(const [key,rows] of groups){rows.sort((a,b)=>{const featured=Number(Boolean(b.offer.featuredHome))-Number(Boolean(a.offer.featuredHome));if(featured)return featured;const pr=(a.seg.priority||999)-(b.seg.priority||999);if(pr)return pr;return (a.offer.ordenWeb||999)-(b.offer.ordenWeb||999);});groups.set(key,rows.slice(0,2));}return groups;}
function notFound(res){res.setHeader('X-Robots-Tag','noindex,nofollow');res.setHeader('Content-Type','text/html; charset=utf-8');return res.status(404).send('Destino de prueba no disponible');}

export default async function handler(req,res){
  if(req.method!=='GET'){res.setHeader('Allow','GET');return res.status(405).send('Method not allowed');}
  const slug=String(req.query.slug||'').trim().toLowerCase();if(!slug||!/^[a-z0-9-]+$/.test(slug))return notFound(res);
  let payload;try{payload=await loadMaster();}catch(_){res.setHeader('X-Robots-Tag','noindex,nofollow');return res.status(503).send('No pudimos cargar la vista previa en este momento.');}
  const destinations=Array.isArray(payload.destinations)?payload.destinations:[],offers=Array.isArray(payload.offers)?payload.offers:[],sources=Array.isArray(payload.sources)?payload.sources:[],hotels=Array.isArray(payload.hotels)?payload.hotels:[],segments=Array.isArray(payload.offerSegments)?payload.offerSegments:[];
  const d=destinations.find(x=>x&&x.slug===slug&&VALID_STATUS.has(x.status));if(!d)return notFound(res);
  const destinationOffers=offers.filter(o=>o&&o.destinationId===d.id),groups=groupsFor(destinationOffers,segments),hasAny=Array.from(groups.values()).some(r=>r.length),image=safe(d.mainImage);
  const quote=new URLSearchParams({travelQuote:'1',cta:'destination_hub_v3',destino:d.name}),quoteUrl=`https://${PUBLIC_HOST}/?${quote.toString()}`,wa=`https://wa.me/523329335952?text=${encodeURIComponent(`Hola, quiero opciones para viajar a ${d.name} con Trhoncal Travel.`)}`;
  const sourceSet=new Set(Array.isArray(d.sourceIds)?d.sourceIds:[]),sourceRows=sources.filter(s=>s&&sourceSet.has(s.id));
  const segmentHtml=SEGMENTS.map(def=>{const rows=groups.get(def.key)||[];if(!rows.length)return '';return `<section class="hub-segment"><div class="hub-segment-head"><div><span class="eyebrow">${esc(def.eyebrow)}</span><h2>${esc(def.title)}</h2></div><p>${esc(def.description)}</p></div><div class="hub-offer-grid">${rows.map(x=>card(x.offer,x.seg,hotels)).join('')}</div></section>`;}).join('');
  const sourceHtml=sourceRows.length?sourceRows.slice(0,6).map(s=>{const u=safe(s.url);return `<div class="hub-source"><div><strong>${esc(s.organization||'Fuente')}</strong><span>${esc(s.title||'')}</span></div>${u?`<a href="${esc(u)}" target="_blank" rel="noopener noreferrer">Fuente oficial ↗</a>`:''}</div>`;}).join(''):'<p>La ficha conserva las fuentes verificadas del destino.</p>';
  res.setHeader('Content-Type','text/html; charset=utf-8');res.setHeader('X-Robots-Tag','noindex,nofollow');res.setHeader('Cache-Control','public, s-maxage=60, stale-while-revalidate=180');
  return res.status(200).send(`<!doctype html><html lang="es"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(d.name)} · Hub de viaje V3 | Trhoncal Travel</title><meta name="robots" content="noindex,nofollow"><link rel="icon" type="image/svg+xml" href="/assets/images/trhoncal-travel-logo.svg"><link rel="stylesheet" href="/assets/css/styles.css"><link rel="stylesheet" href="/assets/css/brand-v2.css"><link rel="stylesheet" href="/assets/css/destination-hub-v2.css"></head><body class="destination-hub-v2"><header class="site-header"><div class="container header-inner"><a class="brand" href="/"><img class="brand-logo" src="/assets/images/trhoncal-travel-logo.svg" alt="Trhoncal Travel"></a><nav class="nav"><a href="/#destinos">Destinos</a><a href="/cuando-viajar/">Cuándo viajar</a><a href="/#promociones">Ofertas</a><a href="/#cotizar">Solicita tu viaje</a></nav><a class="btn btn-outline" href="${wa}" target="_blank" rel="noopener noreferrer">WhatsApp</a></div></header><div class="hub-preview">VISTA PREVIA · HUB COMERCIAL DE DESTINO V3 · la ficha /mexico actual sigue intacta</div><main><section class="hub-hero"><div class="container hub-hero-grid"><div class="hub-hero-copy"><span class="eyebrow">${esc(d.state||'')} · ${esc(d.country||'México')}</span><h1>${esc(d.name)}</h1><p>${esc(d.summary||'')}</p><div class="hub-hero-actions"><a class="btn btn-primary" href="#opciones">Ver opciones listas ↓</a><a class="btn btn-soft" href="${esc(quoteUrl)}">Armar un viaje diferente</a></div></div><div class="hub-hero-image">${image?`<img src="${esc(image)}" alt="${esc(d.imageAlt||d.name)}">`:'<div class="hub-image-placeholder">Imagen del destino</div>'}</div></div></section><section class="hub-products" id="opciones"><div class="container"><div class="hub-products-intro"><div><span class="eyebrow">Primero, viajes concretos</span><h2>Opciones listas para viajar a ${esc(d.name)}</h2></div><p>No son filtros: son configuraciones reales ya cotizadas. Compara la que más se parezca a cómo viajas.</p></div>${hasAny?segmentHtml:`<div class="hub-no-offers"><span class="eyebrow">Todavía no hay una promoción publicada</span><h2>¿Quieres viajar a ${esc(d.name)}?</h2><p>Cuéntanos fechas, adultos, menores y edades. Buscamos opciones reales para ti.</p><a class="btn btn-primary" href="${esc(quoteUrl)}">Quiero cotizar ${esc(d.name)} →</a></div>`}</div></section><section class="hub-custom"><div class="container"><div class="hub-custom-card"><div><span class="eyebrow">¿No encaja ninguna?</span><h2>Estas opciones no son para mi viaje</h2><p>${esc(d.name)} ya queda seleccionado. Dinos fechas, adultos, menores y edades para armar una alternativa a tu medida.</p></div><a class="btn btn-primary" href="${esc(quoteUrl)}">Personalizar mi viaje a ${esc(d.name)} →</a></div></div></section><section class="hub-guide"><div class="container"><div class="hub-guide-head"><div><span class="eyebrow">Después, la información útil</span><h2>Lo que necesitas saber antes de ir</h2></div><p>La investigación del destino sigue aquí para ayudarte a decidir, sólo que ahora aparece después de las opciones reales de viaje.</p></div><div class="hub-accordion"><details open><summary>Por qué ir y para quién funciona</summary><div class="hub-detail-grid"><div><h3>Por qué ir</h3><p>${esc(d.whyGo||'')}</p></div><div><h3>¿Es para ti?</h3><p>${esc(d.travelerProfile||'')}</p></div></div></details><details><summary>Clima, temporadas y cómo llegar</summary><div class="hub-detail-grid"><div><h3>Clima y temporadas</h3><p>${esc(d.climateSeasons||'')}</p></div><div><h3>Cómo llegar y moverse</h3><p>${esc(d.connectivity||'')}</p></div></div></details><details><summary>Atractivos y experiencias</summary><div class="hub-detail-grid"><div><h3>Atractivos clave</h3>${list(d.attractions)}</div><div><h3>Experiencias</h3>${list(d.experiences)}</div></div></details><details><summary>Historia, gastronomía y qué combinar</summary><div class="hub-detail-grid"><div><h3>Historia y contexto</h3><p>${esc(d.history||'')}</p><h3>Gastronomía</h3><p>${esc(d.gastronomy||'')}</p></div><div><h3>Qué combinar</h3>${list(d.combinations)}<h3>Patrimonio</h3><p>${esc(d.sustainabilityHeritage||'')}</p></div></div></details></div></div></section><section class="hub-sources"><div class="container"><span class="eyebrow">Información verificable</span><h2>Fuentes del destino</h2>${sourceHtml}</div></section></main><footer class="footer"><div class="container footer-grid"><div><img class="footer-logo" src="/assets/images/trhoncal-travel-logo.svg" alt="Trhoncal Travel"><p>Opciones reales, información útil y asesoría humana para convertir la idea en viaje.</p></div><div><h3>Explora</h3><p><a href="/#destinos">Destinos</a></p><p><a href="/cuando-viajar/">Cuándo viajar</a></p></div><div><h3>Contacto</h3><p><a href="${wa}" target="_blank">WhatsApp</a></p><p><a href="mailto:viajestroncal@gmail.com">viajestroncal@gmail.com</a></p></div></div></footer></body></html>`);
}
